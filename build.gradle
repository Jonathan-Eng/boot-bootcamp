//project(':logs-shipper')
//project(':indexer')

task start(type: Exec) {
    executable "sh"
    args "-c","./gradlew shadowJar && docker-compose --compatibility up --build"
}

task quit(type: Exec) {
    executable "sh"
    args "-c","docker-compose down && ./gradlew clean"
}